apiVersion: v1
kind: Service
metadata:
  name: anychat-service
spec:
  type: NodePort
  selector:
    app: chat-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080